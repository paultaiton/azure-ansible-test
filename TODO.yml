azure_rm_managed_disk:
  - why is it not idempotent?

azure-storage:
  - package deprecated in pip.

azure_rm_storageaccount:
  storageaccount and info modules have different return values

azure_rm_virtualmachine:
  - can add existing datadisks by id at provision time

azure_rm_virtualmachine_info:
  - lookup by fqid
  - data disk output doesn't contain disk ID or name
  - osdisk??

azure_rm_roleassignment:
  - can use display names (or other?) attributes to make assignments
  OR make module to invoke graph api.

azure_rm_securitygroup:
  - C:\Users\paul\Documents\vscode-workspace\ansible-azure-network\Azure\addCommonServiceRule.py
